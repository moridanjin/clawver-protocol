{
  "name": "@clawver/sdk",
  "version": "0.1.0",
  "description": "TypeScript SDK for the ClawVer Protocol — verified agent execution",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["clawver", "agents", "solana", "sandbox", "verification", "sdk"],
  "license": "MIT",
  "dependencies": {
    "bs58": "^5.0.0",
    "tweetnacl": "^1.0.3"
  },
  "devDependencies": {
    "@types/node": "^22.12.0",
    "typescript": "^5.7.3"
  }
}
